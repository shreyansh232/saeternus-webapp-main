/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['demo.themesberg.com'], // Corrected to use 'domains' and array format
    }
};

export default nextConfig;
